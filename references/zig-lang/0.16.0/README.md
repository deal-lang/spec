# Zig Language Reference 0.16.0

Vendored from: <https://ziglang.org/documentation/0.16.0/>

Fetched at: `2026-05-18T23:57:20Z`

Files:

- `langref.html` - upstream self-contained HTML language reference
- `SHA256SUMS` - local checksums for vendored files

Notes:

- The Zig language reference is published as one self-contained HTML page.
- Zig standard library docs are separate. With Zig installed, run `zig std` to serve them locally.
- Re-run `scripts/download-zig-docs.sh --version 0.16.0 --force` to refresh this copy.
