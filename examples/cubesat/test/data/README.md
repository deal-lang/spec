# Test data (Phase 6)

Evidence artifacts referenced from `model/traceability.dealx`:

- `halcyon-sso-550.tle` — reference two-line elements for the SSO 550 km orbit (input to `eclipse_power.m`)
- `radiometric-cal.csv` — imager radiometric calibration table (GSD/SNR calcs)
- `tvac-report.pdf` — thermal-vacuum correlation report (cited by the pointing-jitter `gap {}` block)
